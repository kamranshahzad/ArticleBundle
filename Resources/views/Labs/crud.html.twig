{% extends themes %}


{% block body %}
<h2> Title of Form </h2>
<form action="{{ path(action) }}" method="post" {{ form_enctype(form) }}>

    {{ form_widget(form)}}

    <div class="form-group">
        <input type="submit" class="btn btn-primary" value="create" />
    </div>

</form>

{% endblock body %}